# MyWora — public environment variables
# Copy this file to .env.local for local overrides.
# Only VITE_* variables are exposed to the client — never store secrets here.

VITE_APP_NAME=MyWora
VITE_APP_URL=http://localhost:5173
